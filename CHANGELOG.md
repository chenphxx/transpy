# CHANGELOG

## V2025.9.13

1. **不再需要把 `.env` 放到 exe 同级目录**: 新增配置对话框, 首次运行缺少凭据时引导填写, 保存到 `%APPDATA%\transpy\config.ini`; 托盘菜单可随时修改, 并支持「测试连接」验证 AK/SK 是否可用。`.env` 与 csv 保留为便携版兜底来源

2. **新增系统托盘图标**: 后台无窗口进程不会再"无处可寻", 托盘右键菜单提供 `设置密钥...` / `暂停翻译热键` / `退出`, 不必再通过任务管理器结束进程

3. 修复潜在的界面崩溃: 原先在 pynput 监听线程中直接创建 tkinter 窗口, 违反 Tk 的线程亲和性, 会偶发窗口不显示或崩溃; 现在 UI 全部收敛到主线程, 后台线程只通过队列投递

4. 修复打包后错误静默消失的问题: `console=False` 时终端提示不可见, 现在致命错误会弹出原生对话框, 并写入 `%LOCALAPPDATA%\transpy\logs\transpy.log`

5. 新增单实例限制: 重复双击 exe 不会再产生多个键盘钩子, 第二个实例会提示并唤起已有实例的设置窗口

6. 翻译弹窗改为按屏幕边界钳制位置, 鼠标靠近屏幕右下角时不再跑出屏幕外

7. 设置进程 AppUserModelID 并写入 exe 版本资源, 任务栏与文件属性正确显示 transpy

8. 提示框改用 tkinter 自绘: 打包为 windowed 进程时原生 `MessageBoxW` 无法作为可靠的交互手段 (可能不显示也不返回), 首次配置向导因此不再依赖它; 向导内支持回车保存、Esc 关闭

9. 新增 `--selftest[=秒数]` 自检模式: 无需任何已有配置即可跑一遍「启动 → 写入配置 → 托盘常驻 → 干净退出」并返回退出码, 便于验证打包产物

10. 打包配置调整: `excludes` 不再排除 `PIL` (托盘图标依赖), `datas` 带上图标资源

## V2026.8.24

1. 认证方式由账号密码改为华为云 AK/SK, 密钥从本地 `.env`/`IAM_transpy-accessKeys.csv` 加载, 不再写入代码 

## V2025.1.16

1. 更名为`transpy` 

## V2024.10.22

1. 结果弹窗将会出现在鼠标的位置上 

## V2024.9.12

1. 增加了错误处理机制, 解决了出现错误时, 由于字典解析失败, 导致程序直接崩溃的问题, 现在会将错误信息显示到结果弹窗中 

2. 将项目上传到了GitHub, 仓库地址如下 

```http
https://github.com/chenphxx/translator
```

## V2024.9.11

1. 为程序设计并添加了图标 
2. 为结果弹窗设置了最小尺寸, 保证logo和标题正常显示 
3. 更改了字体为`Cascadia Code`, 字号`12` 
4. 解决了打包后无法找到程序图标引发的程序崩溃问题 

## V2024.9.10

1. 增加了对目标语言的检测, 检测到中文, 则翻译为英文; 检测到其他语言, 则全部翻译为中文 
2. 解决了当要翻译的日语中含有中文时, 会将其识别为中文, 导致翻译不成功的问题 

## V2024.9.5

1. 更改弹窗为自适应大小, 解决了在文本较多的情况下无法正确显示全部文本的问题 

2. 程序触发改为按下两次`Ctrl`, 解决了在文本编辑器中, 按下两次`T`会将文字覆盖的问题 

3. 改善了使用流程, 按下两次`Ctrl`之后, 程序将自动复制选中的文本, 无需手动复制 

4. 使用`pyinstaller`将程序打包成了`.exe`文件, 现在无需打开IDE即可运行本程序 

## V2024.9.4

1. 添加了自动检测文本源语言的功能, 实现自动中英文互译 

2. 更新了项目结构, 方便后续维护 

## V2024.9.3

第一版脚本开发完成, 连续按下两次按键`T`来触发程序, 程序会从剪贴板中读取文本并且调用API翻译, 因此需要在按下`T`之前, 手动将要翻译的文本复制到剪贴板 

翻译结果将通过弹窗的形式呈现 
